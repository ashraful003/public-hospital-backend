spring:
  application:
    name: public_hospital_backend

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/public_hospital_backend
    username: postgres
    password: ashraful10178

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9090

jwt:
  jwt.secret=12345678901234567890123456789012
  jwt.access.expiration=3600000 
  jwt.refresh.expiration=86400000